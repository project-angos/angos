# E2E overlay: plain push/pull round-trip against angos as a registry.
# Appended to a backend fragment derived from a conformance config so the
# scenario runs against every backend in the matrix.
[global.access_policy]
default_allow = true

[repository."test"]
