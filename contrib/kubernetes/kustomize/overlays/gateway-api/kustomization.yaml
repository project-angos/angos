---
# Gateway API, TLS terminated at the gateway. Before applying, edit
# base/config.toml and fill base/credentials-secret.yaml; the README lists
# what to change.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - httproute.yaml

# components:
#   - ../../components/kubernetes-oidc
#   - ../../components/kubelet-credential-provider
