---
# The registry's own account, so the kubernetes-oidc component can grant the
# JWKS read to it and nothing else in the namespace. Its token stays
# automounted: [auth.oidc.kube] reads it on every fetch.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: angos
  namespace: angos
